# Server Configuration
PORT=8000
HOST=0.0.0.0

# Database Configuration
# For local development (SQLite - no setup needed, default fallback):
# DATABASE_URL=sqlite:///./data/ai_booking.db
# For local PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost/booking_ai_db
# For Render: This is automatically set via render.yaml (fromDatabase)
# DATABASE_URL=postgresql://...  ← Render tự inject, không cần set tay



# Frontend URL (for CORS)
# For local development: http://localhost:3000
# For production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# Email Configuration for BookingAI

# SMTP Settings (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM=noreply@bookingai.com

# Instructions to get Gmail App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to "App passwords" section
# 4. Generate new app password for "Mail"
# 5. Copy the 16-character password and paste above
